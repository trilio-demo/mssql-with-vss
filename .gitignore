# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# Environment / secrets
.env
.env.local
*.env
*.pem
*.key
secrets/
# Filled-in GHCR secret manifests (the *.example.yaml template is safe to commit;
# a real filled copy named ghcr-secret.yaml must never be)
ghcr-secret.yaml
**/ghcr-secret.yaml
vault.yml

# Node (if needed)
node_modules/

# OS
.DS_Store
Thumbs.db

# PDFs — local viewing renders of markdown sources; never commit
*.pdf

# VSCode
.vscode/settings.json

# Claude Code local session config
.claude/

# Project-specific local-only directories — never commit
# (customer/sample docs in collateral/; generated drafts in output/;
#  internal research/positioning docs in private-docs/)
collateral/
output/
private-docs/
# Atlas positioning overview — holds internal context (competitive, customer,
# roadmap) by design; feeds Vince's Atlas/Cockpit tooling, not the public repo
.atlas/

# Local-only files — backlog, scratch pads, meta notes — never commit
TODO.md
*-conversation.md
CLAUDE.local.md
learnings.md
# Session-state archaeology (companion to CLAUDE.md's forward-looking brief);
# holds internal cluster identifiers, IPs, reach commands — local-only
docs/session-state.md
